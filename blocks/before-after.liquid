{% doc %}
  Bloc avant/après avec barre de glissement.

  @example
  {% content_for 'block', type: 'before-after', id: 'before-after' %}
{% enddoc %}

{% liquid
  assign ratio = 1
  case block.settings.image_ratio
    when 'landscape'
      assign ratio = '4 / 3'
    when 'portrait'
      assign ratio = '4 / 5'
    when 'square'
      assign ratio = '1 / 1'
    when 'adapt'
      if block.settings.image_before != blank
        assign ratio = block.settings.image_before.aspect_ratio
      elsif block.settings.image_after != blank
        assign ratio = block.settings.image_after.aspect_ratio
      endif
  endcase

  if ratio == 0 or ratio == null
    assign ratio = 1
  endif
%}

{% capture style %}
  --ratio: {{ ratio }};
{% endcapture %}

{% liquid
  assign media_width_desktop = '100vw'
  assign media_width_mobile = '100vw'
  assign sizes = 'auto, (min-width: 750px) ' | append: media_width_desktop | append: ', ' | append: media_width_mobile
  assign widths = '240, 352, 832, 1200, 1600, 1920, 2560, 3840'
%}

<script src="{{ 'before-after.js' | asset_url }}" type="module"></script>

<before-after-component
  class="before-after-block before-after-block--{{ block.id }}{%- if block.settings.additional_class != blank %} {{ block.settings.additional_class }}{% endif -%}"
  style="
    {{ style }}
    --width-desktop: {{ block.settings.image_width_desktop }}%;
    --width-mobile: {{ block.settings.image_width_mobile }}%;
    --margin-top: {{ block.settings.margin_top }}px;
    --margin-bottom: {{ block.settings.margin_bottom }}px;
  "
  {{ block.shopify_attributes }}
  data-ref="before-after"
  data-default-position="{{ block.settings.default_position }}"
  {% if request.visual_preview_mode %}
    data-visual-preview-mode
  {% endif %}
>
  <div class="before-after-block__container">
    <div class="before-after-block__image before-after-block__image--after">
      {% if block.settings.image_after != blank %}
        {% assign image_alt = block.settings.image_after.alt %}
        {{
          block.settings.image_after
          | image_url: width: 3840
          | image_tag:
            width: block.settings.image_after.width,
            widths: widths,
            height: block.settings.image_after.height,
            class: 'before-after-block__img',
            sizes: sizes,
            alt: image_alt,
            loading: 'lazy'
        }}
      {% else %}
        {% assign default_alt = 'placeholder.placeholder_banner_alt' | t %}
        <img
          src="{{ 'placeholder-product.jpg' | asset_img_url: 'x1200' }}"
          alt="{{ default_alt }}"
          width="1000"
          height="auto"
          class="before-after-block__img"
          loading="lazy"
        >
      {% endif %}
    </div>

    <div class="before-after-block__image before-after-block__image--before" data-ref="before-image">
      {% if block.settings.image_before != blank %}
        {% assign image_alt = block.settings.image_before.alt %}
        {{
          block.settings.image_before
          | image_url: width: 3840
          | image_tag:
            width: block.settings.image_before.width,
            widths: widths,
            height: block.settings.image_before.height,
            class: 'before-after-block__img',
            sizes: sizes,
            alt: image_alt,
            loading: 'lazy'
        }}
      {% else %}
        {% assign default_alt = 'placeholder.placeholder_banner_alt' | t %}
        <img
          src="{{ 'placeholder-image.jpg' | asset_img_url: 'x1200' }}"
          alt="{{ default_alt }}"
          width="1000"
          height="auto"
          class="before-after-block__img"
          loading="lazy"
        >
      {% endif %}
    </div>

    <div class="before-after-block__slider" data-ref="slider">
      <div class="before-after-block__slider-line"></div>
      <div class="before-after-block__slider-handle" data-ref="handle">
        {% render 'icon', icon: 'arrow_range' %}
      </div>
    </div>

    {% if block.settings.label_before != blank %}
      <div class="badge badge--{{ block.settings.label_badge_style }} before-after-block__label before-after-block__label--before">
        {{ block.settings.label_before }}
      </div>
    {% endif %}

    {% if block.settings.label_after != blank %}
      <div class="badge badge--{{ block.settings.label_badge_style }} before-after-block__label before-after-block__label--after">
        {{ block.settings.label_after }}
      </div>
    {% endif %}
  </div>
</before-after-component>

{% stylesheet %}
  .before-after-block {
    width: var(--width-mobile);
    margin-top: var(--margin-top);
    margin-bottom: var(--margin-bottom);
    position: relative;
  }

  .before-after-block__container {
    position: relative;
    width: 100%;
    aspect-ratio: var(--ratio);
    overflow: hidden;
    border-radius: var(--card-border-radius);
  }

  .before-after-block__image {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    overflow: hidden;
  }

  .before-after-block__image--before {
    clip-path: inset(0 50% 0 0);
    transition: clip-path 0.1s ease-out;
  }

  .before-after-block__img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .before-after-block__slider {
    position: absolute;
    top: 0;
    left: 50%;
    width: 4px;
    height: 100%;
    background-color: transparent;
    cursor: ew-resize;
    transform: translateX(-50%);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .before-after-block__slider-line {
    width: 3px;
    height: 100%;
    background-color: var(--color-background);
  }

  .before-after-block__slider-handle {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 40px;
    height: 40px;
    background-color: var(--color-background);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: grab;
    transition: transform 0.1s ease-out;
    border: var(--button-secondary-border-width) solid var(--color-secondary-button-border);
  }

  .before-after-block__slider-handle:active {
    cursor: grabbing;
    transform: translate(-50%, -50%) scale(1.1);
  }

  .before-after-block__slider-icon {
    width: 16px;
    height: 16px;
    color: var(--color-text);
  }

  @media (width > 750px) {
    .before-after-block__slider-handle {
      width: 48px;
      height: 48px;
    }

    .before-after-block__slider-icon {
      width: 20px;
      height: 20px;
    }
  }

  .before-after-block__label {
    position: absolute;
    top: 1rem;
    pointer-events: none;
  }

  .before-after-block__label--before {
    left: 1rem;
  }

  .before-after-block__label--after {
    right: 1rem;
  }

  .before-after-block__label.badge--custom {
    background-color: var(--badge-custom-background);
    color: var(--badge-custom-text);
    border: var(--badge-primary-border-width) solid var(--badge-custom-border);
  }

  @media (width > 750px) {
    .before-after-block {
      width: var(--width-desktop);
    }
  }

  shopify-visual-preview-block-list .before-after-block[data-visual-preview-mode] {
    width: 100%;
    max-width: 600px;
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:before_after",
  "tag": null,
  "settings": [
    {
      "type": "header",
      "content": "t:images"
    },
    {
      "type": "image_picker",
      "id": "image_before",
      "label": "t:image_before"
    },
    {
      "type": "image_picker",
      "id": "image_after",
      "label": "t:image_after"
    },
    {
      "type": "select",
      "id": "image_ratio",
      "label": "t:aspect_ratio",
      "options": [
        {
          "value": "adapt",
          "label": "t:auto"
        },
        {
          "value": "portrait",
          "label": "t:portrait"
        },
        {
          "value": "square",
          "label": "t:square"
        },
        {
          "value": "landscape",
          "label": "t:landscape"
        }
      ],
      "default": "adapt"
    },
    {
      "type": "header",
      "content": "t:labels"
    },
    {
      "type": "text",
      "id": "label_before",
      "label": "t:label_before",
      "default": "t:before"
    },
    {
      "type": "text",
      "id": "label_after",
      "label": "t:label_after",
      "default": "t:after"
    },
    {
      "type": "select",
      "id": "label_badge_style",
      "label": "t:badge_style",
      "options": [
        { "value": "primary", "label": "t:primary" },
        { "value": "secondary", "label": "t:secondary" }
      ],
      "default": "secondary"
    },
    {
      "type": "header",
      "content": "t:behavior"
    },
    {
      "type": "range",
      "id": "default_position",
      "label": "t:default_position",
      "min": 0,
      "max": 100,
      "step": 1,
      "default": 50,
      "unit": "%",
      "info": "t:default_position_info"
    },
    {
      "type": "header",
      "content": "Largeur"
    },
    {
      "type": "range",
      "id": "image_width_desktop",
      "label": "t:image_width_desktop",
      "default": 100,
      "min": 1,
      "max": 100,
      "step": 1,
      "unit": "%"
    },
    {
      "type": "range",
      "id": "image_width_mobile",
      "label": "t:image_width_mobile",
      "default": 100,
      "min": 1,
      "max": 100,
      "step": 1,
      "unit": "%"
    },
    {
      "type": "header",
      "content": "t:margins"
    },
    {
      "type": "range",
      "id": "margin_top",
      "label": "t:margin_top",
      "default": 0,
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "margin_bottom",
      "label": "t:margin_bottom",
      "default": 0,
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px"
    },
    {
      "type": "header",
      "content": "t:advanced_settings"
    },
    {
      "type": "text",
      "id": "additional_class",
      "label": "t:additional_class",
      "info": "t:additional_class_info"
    },
    {
      "type": "header",
      "content": "t:go_further"
    },
    {
      "type": "paragraph",
      "content": "t:go_further_description"
    }
  ],
  "presets": [
    {
      "name": "t:before_after",
      "category": "t:narrative"
    }
  ]
}
{% endschema %}
